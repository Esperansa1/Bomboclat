# CSGORoll Skin Sniper - required environment variables
# Copy to .env and fill in values

# Session cookie value (DevTools → Application → Cookies → session)
CSGOROLL_SESSION=s%3A...

# Cloudflare clearance token (DevTools → Application → Cookies → cf_clearance)
# Expires every ~30 min — refresh manually or Phase 4 automates this via CapSolver
CF_CLEARANCE=P6lsNj8q...

# CapSolver API key (https://dashboard.capsolver.com)
CAPSOLVER_API_KEY=your_key_here
